
describe('/calc', () => {
  test('sending request with username siddharth returns his score');
  test('validation: rejects request if username is not provided');
  test('validation: rejects request if empty username is provided');
  test('validation: rejects request if too long username is provided');
});
